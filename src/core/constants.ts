/** Default cookie name for locale persistence. */
export const DEFAULT_LOCALE_COOKIE = "NEXT_LOCALE";

/** Request header the locale proxy stamps with the resolved locale on every pass-through. */
export const LOCALE_HEADER = "x-next-typed-intl-locale";
