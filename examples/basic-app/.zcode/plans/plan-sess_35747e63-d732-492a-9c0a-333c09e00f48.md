I will completely redesign the app UI/UX to a "top tier" modern aesthetic using a refined vanilla CSS design system in `globals.css`, without adding any new libraries.

1. **CSS Overhaul (`app/globals.css`)**:
   - Implement a comprehensive color palette (Slate, Indigo, Emerald) with multiple shades for depth.
   - Create a multi-layered shadow system for realistic elevation.
   - Refine typography (Inter/JetBrains Mono) with better spacing, weights, and line-heights.
   - Stylize cards with a "bento" feel: soft borders, hover elevations, and subtle background shifts.
   - Polish buttons with vibrant gradients, scale animations, and refined padding.
   - Enhance segmented controls (Theme/Locale) with a professional, cohesive look.

2. **Layout & Component Updates**:
   - **`app/page.tsx`**: Restructure the hero and card sections for better balance and whitespace.
   - **`components/counter.tsx`**: Upgrade the counter display for a more premium, high-impact look.
   - **Header**: unify the theme and locale controls into a sleek, consistent control bar.

3. **Polish**:
   - Add subtle entry animations and micro-interactions (hover/active states).
   - Ensure perfect RTL support and dark mode contrast.

4. **Verification**:
   - Use `agent-browser` to validate the "top tier" style and ensure no regressions in functionality or RTL layout.